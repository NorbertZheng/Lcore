1.9
	代码整理
1.8
	主节点可以运行程序，id为0
1.7
	混合M2M的master和slave。global.h中的#define IS_MASTER
1.6：
	支持单机版和多机版，修改方式：global.h中的#define IS_M2M
1.5：
	段寄存器改进：放在低20位，减少移位


此版本可运行x86程序，速度很慢，待改进
改进1：未完成。较麻烦，上下文切换时会影响寄存器
	返回块信息（flag、type、spc、cpc）方式的改变。旧版本是在x86中将块信息压入mips堆栈，然后在mips环境中将其取出再做处理。改进为：将块信息直接保存到参数寄存器（4个，a0-a3）。
改进2：已完成，提速较明显
	将临时寄存器保存至mips堆栈而不是x86堆栈。
改进3：已完成，不是很明显
	加速中断处理，取颜色值，画点
改进4：已完成，略有提高
	加速x86push、pop处理。SS段寄存器直接对应低20位
改进5：
	加速画图，将画矩形函数封装成一个中断
改进6：加速硬件cache的软件管理，自制cache.o
改进7：给x86分配新的sp
8：flag寄存器放在低16位

1.4:
	中断改进：仅保存相关寄存器
	中断处理完后继续向下执行，不跳回转移处理